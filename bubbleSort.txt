 { int i, a[10]; 
     i := 0; while (10 - i) { read a[i]; i:= i+1 };
	   { int is, c; is := 1; 
       while (is) { is := 0; i:= 0;
         while (9 - i) {
           if (a[i]-a[i+1]) {
             is:=1; c:=a[i+1]; a[i+1]:=a[i]; a[i]:=c };
	       i:= i+1 } }
     };  
     i := 0; while (10 - i) {write a[i]; i:= i+1}
}  
 